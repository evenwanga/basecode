# Spring Boot local profile datasource settings
SPRING_PROFILES_ACTIVE=local
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/user_center
SPRING_DATASOURCE_USERNAME=user_center
SPRING_DATASOURCE_PASSWORD=change_me

# Dedicated PostgreSQL instance (docker) to avoid conflicts
PG_CONTAINER_NAME=user-center-pg
PG_HOST_PORT=5433
PG_DB=user_center
PG_USER=user_center
PG_PASSWORD=change_me
# Launch example:
# docker run --name ${PG_CONTAINER_NAME} -e POSTGRES_DB=${PG_DB} -e POSTGRES_USER=${PG_USER} -e POSTGRES_PASSWORD=${PG_PASSWORD} -p ${PG_HOST_PORT}:5432 -d postgres:15

# LLM Configuration (Qwen)
LLM_PROVIDER=qwen
LLM_API_KEY=sk-dc3c212006c547509799a10ffe1fea6b
LLM_API_URL=
LLM_MODEL=qwen-plus
LLM_TIMEOUT=30

# Redis Configuration
REDIS_HOST=saleschampion-redis
REDIS_PORT=6379
REDIS_PASSWORD=SalesChampion_Redis_2024!Secure
REDIS_DB=1